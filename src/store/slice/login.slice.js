import { createSlice } from "@reduxjs/toolkit";

export const isLoggedSlice = createSlice({
  name: "islogged",
  initialState: false,
  reducers: {},
});

export const {} = isLoggedSlice.actions;

export default isLoggedSlice.reducer;
